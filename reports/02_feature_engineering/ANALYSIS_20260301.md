# Feature Engineering - Spotify Growth ML

**Fecha del análisis:** 2026-03-01  
**Versión del dataset de entrada (interim):** `spotify_tracks_interim_20260301_000321.parquet`  
**Versión del dataset procesado:** `spotify_model_ready_20260301_005928.parquet`  
**Notebook:** `notebooks/02_feature_engineering.ipynb`

---

## 📊 Resumen Ejecutivo

- El notebook fue diseñado como etapa de **consolidación de features**. 
- Entrada: `66,156` filas x `26` columnas.
- Filtro de población aplicado: `0.5 <= duration_min <= 15`.
  - Filas removidas: `82` (`0.12%`).
  - Filas post-filtro: `66,074`.
- Set final cerrado de modelado: `10` features seleccionadas en EDA (P70).
- Imputación determinística por mediana en features numéricas:
  - Nulos antes: `27,141`.
  - Nulos después: `0`.
  - Nulos resueltos: `100.00%`.
- Dataset final exportado: `66,074` filas x `13` columnas (esquema mínimo model-ready).
- Target `popular_high` (P70) quedó consistente:
  - Antes del filtro: `{0: 45,560, 1: 20,596}`.
  - Después del filtro: `{0: 45,479, 1: 20,595}`.
- Compatibilidad con `03_modeling.ipynb` validada (`X` usable con 10 features, split estratificado OK).

> En esta etapa no se regeneran features seleccionadas por EDA; se consolida el set final para modelado.

---

## 🔧 Transformaciones Aplicadas

### 1. Carga y validación de contrato
- Carga automática del `interim` más reciente en `data/interim`.
- Validaciones obligatorias:
  - Presencia de columnas requeridas (`track_id`, `track_popularity`, `duration_ms`, `popular_high` + 10 features seleccionadas).
  - Coherencia del target P70:
    - umbral por quantile: `51.0`
    - umbral inferido desde `popular_high == 1`: `51.0`

### 2. Filtro de población
- Se derivó `duration_min = duration_ms / 60000` **solo para filtrar**.
- Regla aplicada: `0.5 <= duration_min <= 15`.
- Impacto:
  - Antes: `66,156` filas
  - Después: `66,074` filas
  - Removidas: `82` filas (`0.12%`)

### 3. Selección de features finales (set cerrado)
Features usadas en modelado:
1. `artist_avg_popularity`
2. `playlist_avg_popularity`
3. `playlist_popularity_std`
4. `artist_popularity_std`
5. `years_since_release`
6. `album_release_year`
7. `playlist_track_count`
8. `artist_track_count`
9. `artist_avg_duration`
10. `playlist_avg_duration`

### 4. Imputación
- Estrategia: mediana por columna (post-filtro).
- Resumen de imputación:
  - `artist_popularity_std`: mediana `14.138768`, nulos `11,015`.
  - `playlist_avg_popularity`: mediana `35.808511`, nulos `4,027`.
  - `playlist_popularity_std`: mediana `19.321796`, nulos `4,027`.
  - `playlist_track_count`: mediana `209.0`, nulos `4,027`.
  - `playlist_avg_duration`: mediana `3.359414`, nulos `4,027`.
  - `album_release_year`: mediana `2021.0`, nulos `9`.
  - `years_since_release`: mediana `4.470910`, nulos `9`.
- Nulos totales en las 10 features:
  - Antes: `27,141`
  - Después: `0`

---

## 🎯 Target Variable

- Variable: `popular_high` (heredada desde EDA, percentil 70).
- Umbral validado: `51.0`.
- Distribución antes del filtro:
  - No popular (0): `45,560` (`68.87%`)
  - Popular (1): `20,596` (`31.13%`)
- Distribución después del filtro:
  - No popular (0): `45,479` (`68.83%`)
  - Popular (1): `20,595` (`31.17%`)
- Conclusión: el filtro no altera materialmente el balance de clases.

---

## 🧱 Dataset Procesado (Salida)

Esquema exacto exportado (`13` columnas):
- IDs/meta: `track_id`, `track_popularity`
- Features finales: 10 columnas (listadas arriba)
- Target: `popular_high`

Verificaciones:
- Shape: `66,074 x 13`
- Nulos totales: `0`
- Nulos en features finales: `0` en todas las columnas

---

## ✅ Compatibilidad con Modelado

Prueba de contrato con lógica de `03_modeling.ipynb`:
- `X = df.drop(columns=["track_id", "track_popularity", "popular_high"])`
- `y = df["popular_high"]`
- Resultado:
  - `X shape`: `(66,074, 10)`
  - `X_train`: `(52,859, 10)`
  - `X_test`: `(13,215, 10)`
  - split estratificado: OK

---

## 💡 Conclusiones Principales

1. La etapa de feature engineering quedó alineada al contrato EDA→FE: no hay recalculo redundante de features seleccionadas.
2. El dataset final queda minimalista y estable para modelado (13 columnas exactas).
3. La imputación por mediana resolvió completamente los nulos de las features finales sin necesidad de técnicas adicionales en esta iteración.
4. El filtro de duración tuvo impacto bajo en volumen (`0.12%`) y mantuvo estable la distribución del target.
5. El output es directamente consumible por `03_modeling.ipynb` sin ajustes de esquema.

---

## ⚠️ Limitaciones y Consideraciones

- La imputación por mediana es una regla simple; no modela dependencia entre variables.
- Se mantiene un set cerrado de 10 features: no se evaluaron nuevas candidatas en esta iteración.
- El filtro de duración elimina extremos; esto mejora homogeneidad pero puede excluir casos de negocio particulares.

---

## 📈 Próximos Pasos

1. Ejecutar y completar `notebooks/03_modeling.ipynb` sobre `spotify_model_ready_20260301_005928.parquet`.
2. Comparar desempeño del baseline con este set cerrado y, si es necesario, abrir una iteración controlada para nuevas features.
3. Documentar métricas finales de modelado en `reports/03_modeling/ANALYSIS.md`.

---

## 📎 Referencias

- Reporte HTML completo: `report_latest.html`
- Dataset de entrada (interim): `data/interim/spotify_tracks_interim_20260301_000321.parquet`
- Dataset procesado generado: `data/processed/spotify_model_ready_20260301_005928.parquet`
- Notebook ejecutado: `notebooks/02_feature_engineering.ipynb`
